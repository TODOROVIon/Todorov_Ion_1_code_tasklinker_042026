security:

    password_hashers:
        App\Entity\Users: 'auto'

        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
            algorithm: 'auto'
            # Demander Patxi pour explications
            # cost: 15 # for bcrypt (default is 13)
            # time_cost: 4 # for argon2i (default is 2)
            # memory_cost: 1024 # for argon2i (default is 1024)
    
    firewalls:
        main:
            pattern: ^/admin